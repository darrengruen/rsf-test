import * as acts from './actions'
import * as cons from './constants'
import red from './reducers'

export const actions = acts
export const constants = cons
export const reducers = red

export default { actions, constants, reducers }
